name: Zen RSS Generator

    echo "Зависимости установлены"
on:
  schedule:
    - cron: '0 */2 * * *'  # Каждые 2 часа
  workflow_dispatch:  # Запуск вручную через кнопку

jobs:
  generate-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        ref: main
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install pytz
        echo "Зависимости установлены"
    
    - name: Generate RSS feed
      run: |
        python generate_rss.py
        cat feed.xml  # Показать содержимое для проверки
    
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_branch: gh-pages
        publish_dir: ./
        keep_files: false
        force_orphan: true
    
    - name: Success message
      run: |
        echo "🚀 RSS-лента успешно обновлена!"
        echo "🔗 Доступна по адресу: https://$GITHUB_ACTOR.github.io/zen-rss-auto/feed.xml"
 name: Install dependencies
  run: |
    pip install -r requirements.txt
    echo "Зависимости установлены"
